import { SliderItemDirective } from './slider-item.directive';

describe('SliderItemDirective', () => {
  it('should create an instance', () => {
    // const directive = new SliderItemDirective();
    // expect(directive).toBeTruthy();
  });
});
