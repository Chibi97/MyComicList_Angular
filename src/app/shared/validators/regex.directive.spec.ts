import { RegexDirective } from './regex.directive';

describe('RegexDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexDirective();
    expect(directive).toBeTruthy();
  });
});
